# React 18 adapter

Confirm state ownership before adding hooks. Preserve effect cleanup, stable dependencies, controlled/uncontrolled conventions, and existing query/form libraries. Avoid derived state duplication. Test through accessible roles and user actions; treat Strict Mode double invocation as a signal, not something to silence blindly.
