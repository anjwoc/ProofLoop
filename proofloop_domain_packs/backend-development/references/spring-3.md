# Spring Boot 3 adapter

Confirm controller, validation, service, repository, exception-advice, and transaction conventions. Preserve Jakarta validation and response mappings already used by neighboring endpoints. Do not move transaction ownership without tracing proxy boundaries. Prefer repository-provided slice or integration test conventions and verify security filters when an endpoint is protected.
