# Pytest adapter

Follow local fixture scopes and markers. Avoid hidden autouse coupling, mutable session fixtures, and broad monkeypatches. Use `tmp_path`, deterministic parametrization, and repository async conventions. Run the node ID or focused file before the relevant suite.
