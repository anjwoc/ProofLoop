# Jest adapter

Restore timers and mocks, distinguish module reset from behavior isolation, await asynchronous outcomes, and avoid snapshots as the sole contract. Prefer Testing Library user-facing queries for UI behavior and repository conventions for fake timers.
