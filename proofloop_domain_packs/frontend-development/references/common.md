# Frontend common proof seams

Trace route → component ownership → state/data source → user action → visible outcome. Cover loading, empty, success, error, disabled, and permission states only where relevant. Preserve semantic HTML, focus, keyboard behavior, accessible naming, and the repository's token system. Prefer focused interaction assertions and browser evidence over implementation snapshots.
